<!--index.wxml-->
<view class="container" bindtouchend="end" bindtouchstart="start">
  <view bindtap="gotoLink"   wx:for="{{examList}}" wx:key="{{item.examId}}">
    <view>{{item.examName}}</view>
    <text>{{item.creatorName}}</text>
  </view>
</view>

